name: my-drupal
recipe: drupal9
config:
  php: "8.3"
  composer_version: 2
  webroot: web
  xdebug: true
services:
  database:
    type: mariadb:10.6 
tooling:
  drush:
    service: appserver
    cmd: drush
  composer:
    service: appserver
  php:
    service: appserver
